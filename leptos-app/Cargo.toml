[package]
name = "leptos-app"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.8", default-features = false, features = ["ssr"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
