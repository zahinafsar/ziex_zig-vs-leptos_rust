pub fn SsrPage(allocator: zx.Allocator) zx.Component {
    const arr: [50]u32 = @splat(1);

    return (
        <main @allocator={allocator}>
            {for (arr, 0..) |v, i| (
                <div>SSR {v}-{i}</div>
            )}
        </main>
    );
}

const zx = @import("zx");
